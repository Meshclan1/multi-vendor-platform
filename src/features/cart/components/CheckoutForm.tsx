// src/features/cart/components/CartCard.tsx

"use client"; // Optional but recommended if there's any client logic

const CheckoutForm = () => {
  return (
    <h1 className="text-6xl text-white font-extrabold">CheckoutForm Page</h1>
  );
};

export default CheckoutForm;
